{
  "name": "travel-system",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci --ignore-scripts 2>/dev/null || npm install --ignore-scripts 2>/dev/null; npm --prefix frontend run build"
  }
}
